[package]
name = "mysqlbinlog-analyzer"
version = "0.1.0"
edition = "2021"
authors = ["Eldad Zack"]
description = "Analyzes the MySQL binlog from the decoded text output"

[dependencies]
anyhow = "1.0.69"
clap = { version = "4.1.9", features = ["derive", "cargo"] }
lazy_static = "1.4.0"
regex = "1.7.1"
